# Sample/reference HTML (large, not source)
.dev_references

# Build output (root-level compiled binary only — not the cmd/manifest or manifest/ packages)
/build
/bin
/manifest.exe

# Extractor output
/out

# Go
*.test
*.out
coverage.out
vendor/

# OS / editor
.DS_Store
Thumbs.db
